function PokemonList() {

    // 포켓몬 정보를 가져와야한다.
    // api 요청을 해서 state에 담아준다.
    
    // const result = useSelector(state => state.pokemonReducer);
    
    return (
        <h1>포켓몬 리스트</h1>
    );
}